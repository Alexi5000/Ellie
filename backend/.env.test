# Test Environment Configuration
NODE_ENV=test
PORT=0
CORS_ORIGIN=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Mock API Keys for Testing
OPENAI_API_KEY=test_openai_api_key_mock
GROQ_API_KEY=test_groq_api_key_mock

# Rate Limiting (more lenient for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Audio Processing
MAX_AUDIO_FILE_SIZE=10485760
ALLOWED_AUDIO_FORMATS=audio/wav,audio/mp3,audio/m4a,audio/webm

# Session Configuration
SESSION_TIMEOUT_MS=1800000

# Logging (reduced for tests)
LOG_LEVEL=error

# Redis Cache Configuration (mock for tests)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_DB=1

# CDN Configuration (disabled for tests)
CDN_ENABLED=false
CDN_BASE_URL=
CDN_REGIONS=us-east-1,eu-west-1
ASSET_VERSION=1.0.0
ASSET_COMPRESSION=false
ASSET_MINIFICATION=false
IMAGE_OPTIMIZATION=false
ASSET_CACHING=false

# Database Configuration (mock for tests)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ellie_test_db
DB_USER=postgres
DB_PASSWORD=
DB_SSL=false
DB_POOL_SIZE=5
DB_CONNECTION_TIMEOUT=10000
DB_IDLE_TIMEOUT=60000