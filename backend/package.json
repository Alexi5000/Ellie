{
  "name": "@ellie/backend-ci-compat",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Compatibility bridge for legacy GitHub Actions that still expect a backend workspace.",
  "scripts": {
    "lint": "cd .. && npx pnpm check",
    "type-check": "cd .. && npx pnpm check",
    "test": "cd .. && npx pnpm test",
    "build": "cd .. && npx pnpm build"
  }
}
