FROM node:20-alpine
LABEL org.opencontainers.image.title="Ellie Legacy CI Compatibility Image"
LABEL org.opencontainers.image.description="Minimal build target used only by legacy GitHub Actions while the production runtime uses the root Dockerfile."
CMD ["node", "-e", "console.log('Ellie legacy CI compatibility image built successfully')"]
